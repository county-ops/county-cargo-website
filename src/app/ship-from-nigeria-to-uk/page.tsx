import ShippingFromNigeriaToUkPage, { metadata as baseMetadata } from '../shipping-from-nigeria-to-uk/page';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  ...baseMetadata,
  alternates: {
    canonical: 'https://countycargo.com/shipping-from-nigeria-to-uk',
  },
};

export default ShippingFromNigeriaToUkPage;
