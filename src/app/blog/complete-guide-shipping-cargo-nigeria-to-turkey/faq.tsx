import React from 'react';
import { HelpCircle } from 'lucide-react';
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/ui/accordion';

const faqs = [
  {
    question: 'How long does air freight take from Lagos to Istanbul Airport (IST)?',
    answer:
      'Air cargo from Lagos (LOS) or Abuja (ABV) to Istanbul Airport (IST) takes 3 to 5 working days for Express Courier and 5 to 8 working days for Standard Air Freight.',
  },
  {
    question: 'Can commercial traders send sample merchandise from Nigeria to Turkey?',
    answer:
      'Yes! Commercial samples, raw agricultural products, and textiles can be shipped with appropriate Commercial Invoices and phytosanitary certificates where required by Turkish customs.',
  },
];

export function Faq() {
  return (
    <section className="py-12 bg-gray-50 border-t border-gray-200">
      <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center gap-2 mb-6">
          <HelpCircle className="w-6 h-6 text-primary" />
          <h2 className="text-2xl font-bold text-secondary">Frequently Asked Questions: Turkey Shipping</h2>
        </div>
        <Accordion type="single" collapsible className="w-full bg-white rounded-xl border border-gray-200 p-4 shadow-2xs">
          {faqs.map((faq, index) => (
            <AccordionItem key={index} value={`item-${index}`}>
              <AccordionTrigger className="text-left font-semibold text-secondary hover:text-primary text-base">
                {faq.question}
              </AccordionTrigger>
              <AccordionContent className="text-gray-700 text-sm leading-relaxed">
                {faq.answer}
              </AccordionContent>
            </AccordionItem>
          ))}
        </Accordion>
      </div>
    </section>
  );
}
