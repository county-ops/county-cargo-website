import React from 'react';
import { HelpCircle } from 'lucide-react';
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/ui/accordion';

const faqs = [
  {
    question: 'Which German cities does County Cargo deliver to from Nigeria?',
    answer:
      'We deliver to all major German cities and metropolitan regions, including Frankfurt, Berlin, Hamburg, Munich, Cologne, Düsseldorf, Stuttgart, and Leipzig.',
  },
  {
    question: 'How long does air freight take from Lagos to Frankfurt Airport (FRA)?',
    answer:
      'Air cargo from Lagos or Abuja to Frankfurt Airport (FRA) takes 3 to 5 working days for Express Courier and 5 to 10 working days for Standard Air Freight.',
  },
];

export function Faq() {
  return (
    <section className="py-12 bg-gray-50 border-t border-gray-200">
      <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center gap-2 mb-6">
          <HelpCircle className="w-6 h-6 text-primary" />
          <h2 className="text-2xl font-bold text-secondary">Frequently Asked Questions: Germany Shipping</h2>
        </div>
        <Accordion type="single" collapsible className="w-full bg-white rounded-xl border border-gray-200 p-4 shadow-2xs">
          {faqs.map((faq, index) => (
            <AccordionItem key={index} value={`item-${index}`}>
              <AccordionTrigger className="text-left font-semibold text-secondary hover:text-primary text-base">
                {faq.question}
              </AccordionTrigger>
              <AccordionContent className="text-gray-700 text-sm leading-relaxed">
                {faq.answer}
              </AccordionContent>
            </AccordionItem>
          ))}
        </Accordion>
      </div>
    </section>
  );
}
