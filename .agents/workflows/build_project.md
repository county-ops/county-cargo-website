# Build Project Workflow

This workflow details the commands required to install dependencies and build the County Cargo (SwiftCargo) application.

## Step 1: Install Dependencies
Run npm install to restore all required packages:
```powershell
npm install
```

## Step 2: Build the Production Application
Run the build script to compile the Next.js application:
```powershell
npm run build
```
